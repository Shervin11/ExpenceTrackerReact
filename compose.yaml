networks:
  nginxproxy_default:
    external: true

services:
  myapp:
    image: romansakh14/expense_tracker_react:latest
    container_name: expense_tracker_react
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    networks:
      - nginxproxy_default
    ports:
      - 5173:5173
